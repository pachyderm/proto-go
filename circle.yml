dependencies:
  override:
    - mkdir -p "${GOPATH}/src/go.pedge.io/proto"
    - rsync -azC --delete ./ "${GOPATH}/src/go.pedge.io/proto/"
    - make -C "${GOPATH}/src/go.pedge.io/proto" testdeps
test:
  override:
    - make -C "${GOPATH}/src/go.pedge.io/proto" test
